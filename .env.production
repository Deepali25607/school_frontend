# PRODUCTION build target — used by `npm run build` / `npm run cap:sync`.
# This is the public Render backend the live Vercel site uses, so the
# distributable APK works on anyone's phone over the internet.
# Drives both REST (lib/api.js) and WebSocket (lib/realtime.js).
VITE_API_URL=https://school-backend-dwwe.onrender.com
