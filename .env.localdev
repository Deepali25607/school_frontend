# LOCAL dev target — used by `npm run cap:sync:local` (vite build --mode localdev).
# Points at this dev machine's LAN IP so a phone on the same Wi-Fi can reach
# the local backend (which runs on port 4002). Update the IP if your Wi-Fi
# address changes.
VITE_API_URL=http://192.168.31.22:4002
