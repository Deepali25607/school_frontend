# LOCAL WEB dev server target — used by `npm run dev` (Vite dev / development mode).
# The browser runs on this same machine, so localhost is correct here
# (the LAN IP in .env.localdev is only for a phone reaching this machine).
# Backend runs on port 4002.
VITE_API_URL=http://localhost:4002
